# Test

test "check" {
}

==>
Program(TestDecl(Test,TestName,Block))

# Test with try

test "check" {
  try check();
}

==>
Program(TestDecl(Test,
                 TestName,
                 Block(Statement(PrefixExpr(PrefixOp(Try),
                                            PrimaryExpr(TypeExpr(Identifier,
                                                                 FnCallArgs)))))))
