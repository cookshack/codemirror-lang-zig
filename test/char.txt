# literal

const ch = 'a';
const constx = 'a';
const xconst = 'a';

==>
Program(Decl(VarDecl(const,
                     Name,
                     Expr(TypeExpr(CharLiteral)))),
        Decl(VarDecl(const,
                     Name,
                     Expr(TypeExpr(CharLiteral)))),
        Decl(VarDecl(const,
                     Name,
                     Expr(TypeExpr(CharLiteral)))))
