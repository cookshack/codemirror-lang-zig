# Empty doc

==>
Program()

# Block statement

fn one() noreturn {
  return;
}

==>
Program(Decl(FnProto(Identifier,ParamDeclList(),TypeExpr(Identifier)),
             Block(Statement(Return))))

# Assign in block

fn one() noreturn {
  a = 1;
}

==>
Program(Decl(FnProto(Identifier,ParamDeclList,TypeExpr(Identifier)),
             Block(Statement(CurlySuffixExpr(TypeExpr(Identifier)),
                             AssignOp,
                             Expr(PrimaryExpr(CurlySuffixExpr(TypeExpr(Integer))))))))

# import

const std = @import("std");

==>
Program
(Decl
 (VarDecl
  (Const,
   Name,
   Expr(PrimaryExpr(CurlySuffixExpr(TypeExpr(BuiltinIdentifier,
                                             FnCallArgs(Expr(PrimaryExpr(CurlySuffixExpr(TypeExpr(StringLiteral))))))))))))
